import example# here we used the example module created previously

result=example.square(3) #here we called the square funcn from example module which we created earlier and accessed the module function using .method( dot method )
print(f"The square is: {result}")

result=example.area(4)
#here also we accessed the area funcn from example module and accessed it using .method